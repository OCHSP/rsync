name: CI skip interop

on:
  workflow_call:

permissions:
  contents: read

jobs:
  interop-with-upstream:
    name: interop with upstream rsync
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - run: echo "Skipped — no code changes"
